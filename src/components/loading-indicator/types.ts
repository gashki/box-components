export enum LoadingIndicatorPosition {
    BOTTOM = 'bottom',
    MIDDLE = 'middle',
    TOP = 'top',
}

export enum LoadingIndicatorSize {
    LARGE = 'large',
    MEDIUM = 'medium',
    REGULAR = 'regular',
    SMALL = 'small',
}

export enum LoadingIndicatorVisibility {
    BLURRED = 'blurred',
    HIDDEN = 'hidden',
    VISIBLE = 'visible',
}
